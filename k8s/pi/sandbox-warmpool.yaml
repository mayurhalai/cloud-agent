apiVersion: extensions.agents.x-k8s.io/v1alpha1
kind: SandboxWarmPool
metadata:
  name: pi-sandbox-warmpool
  labels:
    app: pi-sandbox-warmpool
spec:
  replicas: 1
  sandboxTemplateRef:
    name: pi-sandbox-template
