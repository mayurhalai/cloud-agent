resources:
  - ../base

labels:
  - pairs:
      pool: opencode


namePrefix: opencode-

patches:
  - path: warmpool-template.yaml
    target:
      group: extensions.agents.x-k8s.io
      version: v1alpha1
      kind: SandboxWarmPool
      name: sandbox-warmpool
    
